package com.example.recognitions

import io.flutter.embedding.android.FlutterActivity

/**
 * MainActivity using Flutter v2 Android embedding.
 * No v1 embedding code here — avoids the "deleted Android v1 embedding" build error.
 */
class MainActivity : FlutterActivity()
